# Basic game of rock paper scissors in c++, helped introduce me to maps and vector arrays.

# build commands

Mingw / Windows
```g++ -std=c++17  main.cpp -o main.exe```

Native Compiler
```gcc -std=c++17  main.cpp -o main.exe```
